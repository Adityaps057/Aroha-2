# Netlify Configuration for AROHA
# Minimal valid configuration for React/Vite deployment

[build]
command = "npm run build"
publish = "dist"

# SPA routing: all requests to /index.html for client-side routing
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
